- name: configuring mysql
  hosts: mysql
  become: yes
  tasks: 
  - name: install mysql
    ansible.builtin.package:
     name: mysql-server
     state: present
  - name: services 
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes


